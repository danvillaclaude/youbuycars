# Supabase — the YouBuyCars project (NOT the iSellCars one).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only; used by the inquiry form's server action. Never exposed.
SUPABASE_SERVICE_ROLE_KEY=
